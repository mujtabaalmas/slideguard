[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "slideguard"
version = "0.1.0"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
include = ["ratelimiter*"]
